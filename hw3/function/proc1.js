//Write the PowerA3 (A, B) function 
//that calculates the third power of A and returns it.

const a = 6; //216
const b = 3;

function powerA3(a, b) {
    return Math.pow(a, b);
}

console.log("[" + a + "] to the power of [" + b + "] is: " + powerA3(a, b));